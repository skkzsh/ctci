version: 2
jobs:
  build:
    docker:
      - image: circleci/python
    steps:
      - checkout
      - run:
          command: pip install pytest pyright ruff
      - run:
          command: ruff check
      - run:
          command: pyright
      - run:
          command: pytest
